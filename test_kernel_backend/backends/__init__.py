"""Backend implementations — each sub-package isolates a framework dependency.

Importing a backend sub-package registers it with the global registry.
Only import the backends you have dependencies installed for.
"""
